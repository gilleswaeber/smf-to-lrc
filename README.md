SMF to LRC converter
====================
Extracts the lyrics from a midi file

This a simple project making an interesting use of Kotlin's class extension.

Usage
-----
```
java -jar smftolrc [-v] [<InFile | -> OutFile]
```

### Flags
* `-v` verbose